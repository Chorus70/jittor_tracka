mode: train
debug: False

components:
  data: train_smoke
  transform: finetune_refiner
  model: vm_refiner_pct_neighbor_pointgate_pos_ft19
  system: vm_refiner_pct_neighbor_pointgate_pos_ft19

model:
  num_train_points: 64
  predict_patch_size: 256
  refine_feat_embedding_dim: 96
  refine_pct_attention_channels: 32
  refine_decoder_hidden_dim: 64
  refine_gate_hidden_dim: 64

loss:
  loss: 1.0

optimizer:
  __target__: adam
  lr: 0.00001

trainer:
  epochs: 1
  save_every: 0
  save_every_steps: 0
  loss_ema_alpha: 0.98
