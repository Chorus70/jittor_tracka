mode: train
debug: False

components:
  data: train_fast_b8
  transform: finetune_narrow_noise
  system: vm_p3_unet
  model: vm_unet

loss:
  loss: 1.0

optimizer:
  __target__: adam
  lr: 0.0005

trainer:
  epochs: 25
  save_every: 5
  lr_decay: 0.7
  lr_decay_every: 10
