mode: train
debug: False
load_ckpt: experiments/vm_finetune/checkpoint_19.pkl

components:
  data: train_fast
  transform: finetune_flow_surface
  model: vm_flow_surface_latent_ft
  system: vm_flow_surface_latent_ft

loss:
  loss: 1.0

optimizer:
  __target__: adam
  lr: 0.00001

trainer:
  epochs: 1
  save_every: 0
  save_every_steps: 1
  loss_ema_alpha: 0.98
